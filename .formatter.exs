# Used by "mix format"
[
  import_deps: [:ecto],
  inputs: [
    "{mix,.formatter}.exs",
    "{config,lib,test}/**/*.{ex,exs}",
    "pages/cookbook/**/*.{ex,exs}",
    "examples/**/*.{ex,exs}",
    "scripts/**/*.{ex,exs}"
  ]
]
